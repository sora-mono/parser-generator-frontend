Project(Generator)

add_compile_options("/std:c++latest")

include_directories(${CMAKE_SOURCE_DIR}/src)

add_subdirectory(DfaGenerator)
add_subdirectory(LexicalGenerator)

aux_source_directory(. GENERATOR_SRCS)
add_executable(Generator ${GENERATOR_SRCS})

link_directories(${CMAKE_SOUREC_DIR}/src/Common)
target_link_libraries(Generator common_functions nfa_generator dfa_generator lexical_generator)