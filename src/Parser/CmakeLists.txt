project(Parser)

add_compile_options("/std:c++latest")

add_subdirectory(DfaMachine)
add_subdirectory(SyntaxMachine)

aux_source_directory(. PARSER_SRCS)
add_executable(Parser ${PARSER_SRCS})

link_directories(${CMAKE_SOURCE_DIR}/src/Common)

target_link_libraries(Parser syntax_machine)