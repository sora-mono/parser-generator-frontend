project(Parser)

add_compile_options("/std:c++latest")

add_subdirectory(DfaMachine)
add_subdirectory(SyntaxMachine)

add_library(line_and_column "line_and_column.cpp")

add_executable(Parser "Parser.cpp")

target_link_libraries(Parser syntax_machine dfa_machine line_and_column)